# Placeholder viewer CMake; real sources to be added later

# Keep this directory valid for add_subdirectory() even if empty for now.
# You can later replace this with a proper target, e.g.:
# add_library(viewer STATIC OcctQOpenGLWidgetViewer.cpp OcctQOpenGLWidgetViewer.h)

